const YedekParca1 = ["Kaporta5", "Ak3u", "Stop Lambasi", "Radyator 21", "Karburator 42", "Tekerlek", "3 AYNA", "Jant"];

/*
const BuyukHarf = (pName) => pName.toUpperCase();

const SayiSilme = (pName) => pName.replace(/[0-9]/g,"");

const TersCevirme = (pName) => pName.split("").reverse().join("");

const SirketEkleme = (pName) => "KEREM_AG" + pName;

const Tarih = (pName) => pName + "_" + Date();


const result = YedekParca1.map(BuyukHarf)
                       .map(SayiSilme)
                       .map(TersCevirme)
                       .map(SirketEkleme)
                       .map(Tarih)
console.log("Kerem_AG` de Yedek Parcalar", result);
*/